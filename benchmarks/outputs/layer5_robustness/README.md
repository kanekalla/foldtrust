# Layer 5: Robustness Analysis Outputs

Generated by: `foldtrust benchmark robustness` (or `python3 run_layer5.py`)

Date: 2026-09-25

## Files

### Core Results

- **`robustness_summary.csv`** — All cases × all conditions × all metrics. Columns:
  - `case`: Case name (cftr-5utr, hcv-ires-dii, mapt-e10, sars2-fse, smn2-iss-n1)
  - `condition`: Condition identifier (param_*, temp_*, jitter_*)
  - `tier_agreement`: Fraction of nucleotides with unchanged tier (0–1)
  - `mea_jaccard`: Jaccard index of MEA structure base pairs (0–1)
  - `mfe_jaccard`: Jaccard index of MFE structure base pairs (0–1)
  - `unpaired_spearman`: Spearman correlation of unpaired probabilities (-1 to 1)
  - `firm_retention`: Fraction of baseline FIRM pairs that remain FIRM (0–1)
  - `floppy_retention`: Fraction of baseline FLOPPY pairs that remain FLOPPY (0–1)
  - `mfe_energy`: MFE energy in kcal/mol

- **`robustness_detailed.json`** — Full per-case results including baseline metrics and all condition metrics. Use for programmatic analysis.

- **`mfe_energies_by_params.csv`** — MFE energy per case per parameter set (Turner2004, Andronescu2007, Langdon2018). Verifies parameter loading works correctly.

- **`region_stability.csv`** — Per-case mean and standard deviation of tier agreement across all conditions. Identifies most/least stable cases.

### Figures

Located in `../figures/`:

- **`layer5_tier_agreement_heatmap.png`** — Cases (rows) × conditions (columns) heatmap of per-nucleotide tier agreement. Green = stable (agreement ≈ 1), red = unstable.

- **`layer5_mea_jaccard_heatmap.png`** — Cases × conditions heatmap of MEA structure Jaccard index. Measures base-pair set overlap.

## Tier Classification Logic

Follows FoldTrust's MEA-based tier logic:
- Compute MEA (Maximum Expected Accuracy) structure from base-pair probability matrix
- Positions paired in MEA structure get their pair's tier
- Tier of pair (i,j) based on p(i,j): FIRM ≥ 0.85, SOFT ≥ 0.5, FLOPPY < 0.5
- Unpaired positions are UNPAIRED

This differs from max-probability classification: a position is classified based on whether it's paired in the MEA structure and what that pair's probability is, not on its maximum pairing probability across all possible partners.

## Conditions Tested

### Parameter Sets
- **Andronescu2007** — Andronescu et al. 2007 parameters (CG optimization)
- **Langdon2018** — Langdon et al. 2018 parameters (genetic programming)
- Baseline: **Turner2004**

### Temperatures
- 24°C (cool)
- 30°C (moderate)
- 37°C (baseline, physiological)
- 42°C (fever)
- 45°C (heat shock)

### Window Jitter
- Extend left/right/both by 10 nt
- Extend left/right/both by 25 nt
- Shrink left/right by 10 nt
- Shrink left/right by 25 nt

Flanking sequences fetched from NCBI E-utilities using source accessions from case metadata.

## Command

```bash
foldtrust benchmark robustness -o benchmarks/outputs
```

Or directly:

```bash
python3 run_layer5.py
```

## Key Results

**Temperature is most robust:** Tier agreement 82%, MEA Jaccard 91%, unpaired Spearman 99%. Physiological temperature variations produce consistent tier classifications.

**Parameter sets show moderate divergence:** Tier agreement 53%, but FLOPPY pairs remain FLOPPY (99.8% retention). Low-confidence regions are consistently low.

**Window jitter has largest impact:** MEA Jaccard 22%, FIRM retention 28%. Window boundaries dramatically affect structure predictions.

**Most stable case:** CFTR 5' UTR (tier agreement 65%)  
**Least stable case:** SARS-CoV-2 FSE (tier agreement 55%, pseudoknot region)

## References

See `docs/benchmark/layer5_robustness.md` for full methods, results, interpretation, and citations.
